/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // لا تستخدم experimental.appDir هنا؛ الـ App Router مفعل تلقائياً داخل مجلد /app
};

export default nextConfig;
